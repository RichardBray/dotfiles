(defsrc
  a b c lctl
)

(defvar
  tap-timeout 200
  hold-timeout 200
)

(deflayer base
  (tap-hold $tap-timeout $hold-timeout x lsft)
  y z
  (layer-switch editor)
)

(deflayer editor
  q w e
  (layer-switch base)
)
